[mysql]
host = localhost
user = DBAdmin
password = UserDB2025
database = mysql
port = 3306
charset = utf8mb4
tables = UserDB_main_1,
[ddl]
default = CREATE TABLE `{table_name}` (
    id INT AUTO_INCREMENT PRIMARY KEY,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
UserDB_main_1 = CREATE TABLE UserDB_main_1(
    型号 INT NOT NULL,
    编号 INT NOT NULL,
    位置 VARCHAR(100) NOT NULL,
    创建时间 TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
